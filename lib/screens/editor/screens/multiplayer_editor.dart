
import 'package:flutter/material.dart';

class MultiplayerEditor extends StatelessWidget {
  final String name;

  const MultiplayerEditor({Key key, this.name}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
